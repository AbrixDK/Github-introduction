name: Extract Crown Prince handshake

on:
  pull_request:
    paths:
      - 'handshake/mbs.py'
      - '.github/workflows/handshake-mbs.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Install video tools
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core
      - name: Extract frames
        run: python handshake/mbs.py
      - uses: actions/upload-artifact@v4
        with:
          name: handshake-mbs
          path: |
            handshake/mbs_output/sheets
            handshake/mbs_output/metadata.json
          if-no-files-found: error
          retention-days: 7
