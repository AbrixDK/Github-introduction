name: Handshake observation media

on:
  pull_request:
    paths:
      - 'handshake/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install video tools
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core

      - name: Download clips and extract contact sheets
        run: python handshake/extract.py

      - name: Upload screening sheets
        uses: actions/upload-artifact@v4
        with:
          name: handshake-screening-sheets
          path: |
            handshake/output/sheets
            handshake/output/metadata.json
          if-no-files-found: error
          retention-days: 7
