name: Extract Finland meeting handshake

on:
  pull_request:
    paths:
      - 'handshake/niinisto.py'
      - '.github/workflows/handshake-niinisto.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Install video tools
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core
      - name: Extract frames
        run: python handshake/niinisto.py
      - uses: actions/upload-artifact@v4
        with:
          name: handshake-niinisto
          path: |
            handshake/niinisto_output/sheets
            handshake/niinisto_output/metadata.json
          if-no-files-found: error
          retention-days: 7
